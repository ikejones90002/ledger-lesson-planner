/**
 * Standards frameworks available in the picker. This is intentionally
 * lightweight — a framework name plus a free-text code field — rather
 * than a full standards database, so it works for any subject/grade
 * without needing to ship (and maintain) the full text of each
 * framework. Add real code lookups here later if you want autocomplete.
 */
export const STANDARDS_FRAMEWORKS = [
  'Common Core',
  'California (CA CCSS / CA Frameworks)',
  'Tennessee Academic Standards',
  'Other / State-specific'
]
